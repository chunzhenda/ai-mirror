- 固件子模块采用 `board_xxx.c/h` 成对命名约定（buttons、rgb、wifi_prov），将驱动与接口分离声明。
- 可配置项通过 `Kconfig.projbuild` 暴露到 `menuconfig`，由 `idf_component.yml` 管理组件依赖，构建产物符号名与 CMakeLists.txt 中的 `EMBED_FILES` 保持一致。
- README 中英文版本严格一一对应，内容逐段翻译而非独立编写，确保双语信息同步。